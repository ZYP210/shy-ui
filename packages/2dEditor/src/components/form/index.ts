import { withInstall } from '../../utils'
import upload from './upload.vue'
import cardItem from './CardItem.vue'

export const Upload = withInstall(upload)
export const CardItem = withInstall(cardItem)
